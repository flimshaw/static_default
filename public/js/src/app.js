// Generated by CoffeeScript 1.3.3
(function() {
  var Album, AlbumView, Albums, AlbumsView, Buffer, _ref, _ref1, _ref2, _ref3, _ref4, _ref5,
    __hasProp = {}.hasOwnProperty,
    __extends = function(child, parent) { for (var key in parent) { if (__hasProp.call(parent, key)) child[key] = parent[key]; } function ctor() { this.constructor = child; } ctor.prototype = parent.prototype; child.prototype = new ctor(); child.__super__ = parent.prototype; return child; },
    __bind = function(fn, me){ return function(){ return fn.apply(me, arguments); }; };

  this.app = (_ref = window.app) != null ? _ref : {};

  Album = (function(_super) {

    __extends(Album, _super);

    function Album() {
      return Album.__super__.constructor.apply(this, arguments);
    }

    return Album;

  })(Backbone.Model);

  this.app.Album = Album;

  this.app = (_ref1 = window.app) != null ? _ref1 : {};

  Albums = (function(_super) {

    __extends(Albums, _super);

    function Albums() {
      return Albums.__super__.constructor.apply(this, arguments);
    }

    Albums.prototype.model = app.Album;

    Albums.prototype.url = '/albums';

    return Albums;

  })(Backbone.Collection);

  this.app.Albums = new Albums();

  this.app = (_ref2 = window.app) != null ? _ref2 : {};

  AlbumView = (function(_super) {

    __extends(AlbumView, _super);

    function AlbumView() {
      return AlbumView.__super__.constructor.apply(this, arguments);
    }

    AlbumView.prototype.tagName = 'li';

    AlbumView.prototype.initialize = function() {
      this.template = _.template($("#album_template").html());
      return this.model.bind('change', this.render, this);
    };

    AlbumView.prototype.render = function() {
      var renderedContent;
      renderedContent = this.template(this.model.toJSON());
      $(this.el).html(renderedContent);
      return this;
    };

    return AlbumView;

  })(Backbone.View);

  AlbumsView = (function(_super) {

    __extends(AlbumsView, _super);

    function AlbumsView() {
      return AlbumsView.__super__.constructor.apply(this, arguments);
    }

    AlbumsView.prototype.tagName = 'div';

    AlbumsView.prototype.className = 'albums';

    AlbumsView.prototype.initialize = function() {
      this.template = _.template($('#albums_template').html());
      return this.collection.bind('reset', this.render, this);
    };

    AlbumsView.prototype.render = function() {
      var $albums,
        _this = this;
      $(this.el).html(this.template({}));
      $albums = this.$('.albums');
      this.collection.each(function(album) {
        var view;
        view = new AlbumView({
          model: album,
          collection: _this.collection
        });
        return $albums.append(view.render().el);
      });
      return this;
    };

    return AlbumsView;

  })(Backbone.View);

  this.app.AlbumView = AlbumView;

  this.app.AlbumsView = AlbumsView;

  this.app = (_ref3 = window.app) != null ? _ref3 : {};

  Buffer = (function() {

    function Buffer() {
      this.add = __bind(this.add, this);

    }

    Buffer.prototype.commands = [];

    Buffer.prototype.add = function(fn) {
      var next,
        _this = this;
      next = function() {
        _this.commands.shift();
        if (_this.commands.length) {
          return _this.commands[0](next);
        }
      };
      this.commands.push(fn);
      if (this.commands.length === 1) {
        fn(next);
      }
      return this.commands.length;
    };

    return Buffer;

  })();

  this.app = (_ref4 = window.app) != null ? _ref4 : {};

  jQuery(function() {
    var AppRouter;
    AppRouter = (function(_super) {

      __extends(AppRouter, _super);

      function AppRouter() {
        return AppRouter.__super__.constructor.apply(this, arguments);
      }

      AppRouter.prototype.routes = {
        '': 'home',
        'blank': 'blank'
      };

      AppRouter.prototype.initialize = function() {
        return this.$container = $('#container');
      };

      AppRouter.prototype.home = function() {
        this.$container.empty();
        this.view = new app.AlbumsView({
          collection: app.Albums
        });
        return this.$container.html(this.view.render().el);
      };

      return AppRouter;

    })(Backbone.Router);
    return app.AppRouter = AppRouter;
  });

  this.app = (_ref5 = window.app) != null ? _ref5 : {};

  jQuery(function() {
    app.Albums.fetch();
    app.router = new app.AppRouter();
    app.router.on("route", function() {
      return console.log("routed.");
    });
    Backbone.history.start();
    return console.log("Backbone loaded.");
  });

}).call(this);
