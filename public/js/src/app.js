// Generated by CoffeeScript 1.3.3
(function() {
  var Buffer, Defaults, Model, _ref, _ref1, _ref2, _ref3, _ref4, _ref5,
    __hasProp = {}.hasOwnProperty,
    __extends = function(child, parent) { for (var key in parent) { if (__hasProp.call(parent, key)) child[key] = parent[key]; } function ctor() { this.constructor = child; } ctor.prototype = parent.prototype; child.prototype = new ctor(); child.__super__ = parent.prototype; return child; },
    __bind = function(fn, me){ return function(){ return fn.apply(me, arguments); }; };

  this.app = (_ref = window.app) != null ? _ref : {};

  Model = (function(_super) {

    __extends(Model, _super);

    function Model() {
      return Model.__super__.constructor.apply(this, arguments);
    }

    return Model;

  })(Backbone.Model);

  this.app.Model = Model;

  this.app = (_ref1 = window.app) != null ? _ref1 : {};

  Defaults = (function(_super) {

    __extends(Defaults, _super);

    function Defaults() {
      return Defaults.__super__.constructor.apply(this, arguments);
    }

    Defaults.prototype.model = app.Project;

    Defaults.prototype.url = '/defaults';

    return Defaults;

  })(Backbone.Collection);

  this.app.Defaults = new Defaults;

  this.app = (_ref2 = window.app) != null ? _ref2 : {};

  jQuery(function() {
    var DefaultView, DefaultsView;
    DefaultView = (function(_super) {

      __extends(DefaultView, _super);

      function DefaultView() {
        return DefaultView.__super__.constructor.apply(this, arguments);
      }

      DefaultView.prototype.tagName = 'li';

      DefaultView.prototype.initialize = function() {
        this.template = _.template($("#default_template").html());
        return this.model.bind('change', this.render, this);
      };

      DefaultView.prototype.render = function() {
        var renderedContent;
        renderedContent = this.template(this.model.toJSON());
        $(this.el).html(renderedContent);
        return this;
      };

      return DefaultView;

    })(Backbone.View);
    DefaultsView = (function(_super) {

      __extends(DefaultsView, _super);

      function DefaultsView() {
        return DefaultsView.__super__.constructor.apply(this, arguments);
      }

      DefaultsView.prototype.tagName = 'div';

      DefaultsView.prototype.className = 'defaults';

      DefaultsView.prototype.initialize = function() {
        this.template = _.template($('#defaults_template').html());
        return this.collection.bind('reset', this.render, this);
      };

      DefaultsView.prototype.render = function() {
        var _this = this;
        $(this.el).html(this.template({}));
        this.collection.each(function(project) {
          var view;
          view = new ProjectView({
            model: project,
            collection: _this.collection
          });
          return $projects.append(view.render().el);
        });
        return this;
      };

      return DefaultsView;

    })(Backbone.View);
    app.DefaultView = DefaultView;
    return app.DefaultsView = DefaultsView;
  });

  this.app = (_ref3 = window.app) != null ? _ref3 : {};

  Buffer = (function() {

    function Buffer() {
      this.add = __bind(this.add, this);

    }

    Buffer.prototype.commands = [];

    Buffer.prototype.add = function(fn) {
      var next,
        _this = this;
      next = function() {
        _this.commands.shift();
        if (_this.commands.length) {
          return _this.commands[0](next);
        }
      };
      this.commands.push(fn);
      if (this.commands.length === 1) {
        fn(next);
      }
      return this.commands.length;
    };

    return Buffer;

  })();

  this.app = (_ref4 = window.app) != null ? _ref4 : {};

  jQuery(function() {
    var AppRouter;
    AppRouter = (function(_super) {

      __extends(AppRouter, _super);

      function AppRouter() {
        return AppRouter.__super__.constructor.apply(this, arguments);
      }

      AppRouter.prototype.routes = {
        '': 'home',
        'blank': 'blank'
      };

      AppRouter.prototype.initialize = function() {
        return this.$container = $('#container');
      };

      AppRouter.prototype.home = function() {
        this.$container.empty();
        this.view = new app.ProjectsView({
          collection: app.Projects
        });
        this.$container.html(this.view.render().el);
        return app.setNav('');
      };

      return AppRouter;

    })(Backbone.Router);
    return app.AppRouter = AppRouter;
  });

  this.app = (_ref5 = window.app) != null ? _ref5 : {};

  jQuery(function() {
    app.Projects.fetch();
    app.router = new app.appRounter();
    app.router.on("route", function() {
      return console.log("routed.");
    });
    Backbone.history.start();
    return console.log("Backbone loaded.");
  });

}).call(this);
